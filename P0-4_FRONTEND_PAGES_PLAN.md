# P0-4 前端三大页面详细实施方案

## 📍 当前状态

### 已完成

- ✅ P0-1: Core分账逻辑 (100%)
- ✅ P0-2: OTC查询接口 (100%)
- ✅ P0-3: 前端授权+购买双步骤 (100%)

### 现有UI结构

- 首页（overview）: ✅ 已完成
- 团队页（team）: ✅ 已完成
- 市场页（otc）: ⚠️ 需要完整实现
- 兑换页（swap）: ✅ 已完成
- 记录页（mine）: ✅ 已完成

---

## 🎯 P0-4 五大任务

### Task 1: 节点购买页面 (1.5 小时)

**页面位置**: `overview` 标签页中的现有节点卡片

**当前状态**:

- ✅ 基础卡片存在
- ✅ 购买逻辑存在
- ❌ 缺少详细信息展示
- ❌ 缺少限购说明

**改动内容**:

1. 在节点卡片中添加更多信息:
   - 我的状态: "未购买" / "已购买" / "可升级到超级节点"
   - 计数器: "剩余购买次数：1次"
   - 升级提示: 如果已购买节点，显示"可升级到超级节点"

2. 更新UI布局:
   ```
   ┌─────────────────────────────┐
   │ 节点购买                      │
   ├─────────────────────────────┤
   │ 价格: 1000 USDT             │
   │                             │
   │ 说明: 获得节点身份...        │
   │                             │
   │ 我的状态: ✓ 已购买           │
   │ 限制: 最多购买 1 个          │
   │                             │
   │ [升级为超级节点]             │
   │                             │
   └─────────────────────────────┘
   ```

**实现步骤**:

- [ ] 计算并显示用户的节点购买状态
- [ ] 添加限购说明文本
- [ ] 更新按钮标签和禁用条件

---

### Task 2: 超级节点页面 (1.5 小时)

**页面位置**: `overview` 标签页中的现有超级节点卡片

**当前状态**:

- ✅ 基础卡片存在
- ✅ 购买逻辑存在
- ❌ 缺少升级提示
- ❌ 缺少身份展示

**改动内容**:

1. 增强卡片内容:
   - 显示用户当前身份
   - 显示升级路径（若仅持有节点身份）
   - 添加限购说明

2. 更新UI布局:
   ```
   ┌─────────────────────────────┐
   │ 超级节点购买                  │
   ├─────────────────────────────┤
   │ 价格: 3000 USDT             │
   │                             │
   │ 说明: 获得超级节点身份...    │
   │                             │
   │ 当前身份: 节点或普通用户      │
   │ 升级路径: 可直接购买          │
   │ 限制: 最多拥有 1 个          │
   │                             │
   │ [升级/购买]                  │
   │                             │
   └─────────────────────────────┘
   ```

**实现步骤**:

- [ ] 获取用户当前角色
- [ ] 据此决定显示"升级"或"购买"
- [ ] 针对已有超级节点用户禁用按钮
- [ ] 添加身份转移说明

---

### Task 3: OTC市场页面 (2.5 小时)

**页面位置**: 新增 `otc` 标签页（目前为空）

**页面结构**:

```
┌─────────────────────────────────────────────┐
│ 市场页                                       │
├─────────────────────────────────────────────┤
│                                             │
│ 分布: [所有] [节点] [超级节点]              │
│                                             │
│ ┌──────────────────────────────────────┐   │
│ │ 订单 ID  │ 身份  │ 价格      │ 操作  │   │
│ ├──────────────────────────────────────┤   │
│ │ #123     │ 节点  │ 1200 USD │ 买入  │   │
│ │ #124     │ 超级  │ 3100 USD │ 买入  │   │
│ │ #125     │ 节点  │ 1150 USD │ 买入  │   │
│ └──────────────────────────────────────┘   │
│                                             │
│ [上一页] [页码] [下一页]                   │
│                                             │
│ 我的订单:                                   │
│ ┌──────────────────────────────────────┐   │
│ │ 订单 ID  │ 操作                       │   │
│ ├──────────────────────────────────────┤   │
│ │ #999     │ [撤销] [已挂单]           │   │
│ └──────────────────────────────────────┘   │
│                                             │
│ [新建挂单]                                  │
│                                             │
└─────────────────────────────────────────────┘
```

**实现步骤**:

1. **数据获取** (30 mins):
   - [ ] 调用 `getActiveOrderIdsPaginated()` 获取订单列表
   - [ ] 调用 `getOrder()` 获取订单详情
   - [ ] 调用 `getLastTradePrice()` 获取最后成交价
   - [ ] 实现分页逻辑 (每页10条)

2. **公开市场表格** (1 hour):
   - [ ] 显示所有活跃订单
   - [ ] 按角色过滤 (节点/超级节点)
   - [ ] "购买"按钮触发 `fillOrder()`
   - [ ] 成交后刷新列表

3. **我的订单部分** (30 mins):
   - [ ] 展示当前用户的挂单
   - [ ] "撤销"按钮触发 `cancelOrder()`
   - [ ] "新建挂单"按钮打开创建弹窗

4. **新建挂单弹窗** (30 mins):
   - [ ] 选择身份标的（若有多个）
   - [ ] 输入挂单价格（≥最后成交价）
   - [ ] 确认并提交 `createOrder()`
   - [ ] 成功后关闭弹窗

**关键API调用**:

```typescript
// 获取订单列表
const orderIds = await getActiveOrderIdsPaginated(offset, limit);

// 获取订单详情
const order = await getOrder(orderId);

// 购买订单
await fillOtcOrder(orderId);

// 创建订单
await createOtcOrder(identityId, priceUSDT);

// 撤销订单
await cancelOtcOrder(orderId);

// 检查是否已有活跃订单
const hasOrder = await hasActiveOrder(identityId);
```

---

### Task 4: 我的资产综合查询页 (2 小时)

**页面位置**: 新增或扩展现有页面（可在 mine 标签或新增 assets 标签）

**页面结构**:

```
┌─────────────────────────────────────────────┐
│ 我的资产                                     │
├─────────────────────────────────────────────┤
│                                             │
│ 账户信息:                                   │
│ ├─ 身份: 超级节点                           │
│ ├─ 钱包: 0x1234...                         │
│ └─ 创建时间: 2026-04-10                    │
│                                             │
│ 我的矿机:                                   │
│ ├─ 总数: 5 台                               │
│ └─ [查看订单详情]                            │
│    ┌──────────────────────────────────┐   │
│    │ 订单 ID │ 数量 │ 时间 │ 分账状态  │   │
│    ├──────────────────────────────────┤   │
│    │ #001    │ 2   │ xxx  │ ✓ 已分配  │   │
│    │ #002    │ 3   │ xxx  │ ✓ 已分配  │   │
│    └──────────────────────────────────┘   │
│                                             │
│ 我的身份资产:                               │
│ ├─ 节点数: 1 个 → [查看]                    │
│ ├─ 超级节点: 1 个 → [查看]                  │
│ └─ OTC订单: 0 个                          │
│                                             │
│ 我的OTC订单:                                │
│ └─ [查看正在交易中的订单]                   │
│                                             │
│ 最近交易:                                   │
│ ┌──────────────────────────────────┐   │
│ │ 类型 │ 金额 │ 状态 │ 时间 │     │   │
│ ├──────────────────────────────────┤   │
│ │ 购买 │ 200  │ ✓    │ xxx  │ [详情] │
│ │ 升级 │ 2000 │ ✓    │ xxx  │ [详情] │
│ └──────────────────────────────────┘   │
│                                             │
└─────────────────────────────────────────────┘
```

**实现步骤**:

1. **账户信息** (20 mins):
   - [ ] 显示用户地址
   - [ ] 显示用户角色 (调用 `getUserRole()`)
   - [ ] 显示首次连接/绑定推荐人时间

2. **矿机订单列表** (40 mins):
   - [ ] 调用 `getUserMachineOrders()` 获取订单ID列表
   - [ ] 调用 `getMachineOrder()` 获取订单详情
   - [ ] 计算总矿机数
   - [ ] 显示订单表格（可展开查看详情）

3. **身份管理** (30 mins):
   - [ ] 显示拥有的身份数
   - [ ] 若有OTC挂单，调用 `getIdentityActiveOrder()` 显示当前挂单
   - [ ] 提供转移/管理按钮

4. **OTC订单历史** (30 mins):
   - [ ] 从本地state中获取用户的成交历史
   - [ ] 或从链上查询（如合约支持）
   - [ ] 显示最近10条交易

**关键数据获取**:

```typescript
// 获取用户矿机订单
const orderIds = await getUserMachineOrders(address);

// 获取单个订单详情
const order = await getMachineOrder(orderId);

// 获取用户角色
const role = await getUserRole(address);

// 获取用户组身份ID
const identityId = await getUserIdentityId(address);

// 查询该身份的活跃OTC订单
const activeOrderId = await getIdentityActiveOrder(identityId);
```

---

## 📊 工作量估计

| 任务               | 难度 | 时间     | 备注                    |
| ------------------ | ---- | -------- | ----------------------- |
| Task 1: 节点页     | 低   | 1.5h     | 主要是UI调整            |
| Task 2: 超级节点页 | 低   | 1.5h     | 主要是UI调整            |
| Task 3: OTC市场页  | 高   | 2.5h     | 涉及多个API调用和复杂UI |
| Task 4: 资产页     | 中   | 2h       | 数据聚合和展示          |
| **总计**           |      | **7.5h** |                         |

---

## 🔄 UI标准

### 通用样式

- 使用现有 `.card` 和 `.kv-row` 组件
- 表格使用 `.table-wrap` 和 `<table>` 标签
- 按钮使用 `.primary-btn` 或 `.secondary-btn`
- 加载状态显示 "加载中..."

### 分页组件

```typescript
interface PaginationProps {
  total: number;
  pageSize: number;
  current: number;
  onChange: (page: number) => void;
}
```

### 弹窗组件

- 使用现有 `.modal-overlay` 和 `.modal-content` 样式
- 确保手机端适配

---

## ✅ 完成标准

### 节点/超级节点页

- [x] 显示价格和说明
- [x] 展示用户当前身份状态
- [x] 显示限购说明
- [x] 按钮状态正确（购买/升级/禁用）
- [x] 所有操作有浮窗提示

### OTC市场页

- [x] 显示公开订单列表
- [x] 分页功能正常
- [x] 可按角色过滤
- [x] "购买"功能正常
- [x] "我的订单"显示正确
- [x] 撤销订单功能正常
- [x] 新建挂单流程完整
- [x] 所有交易有确认弹窗

### 资产页

- [x] 显示账户基本信息
- [x] 显示矿机订单列表
- [x] 显示身份信息
- [x] 显示OTC订单状态
- [x] 显示交易历史
- [x] 移动端适配正常

---

## 🚀 开发建议

### 优先级

1. **高优先**: Task 1 & 2 (短平快)
2. **中优先**: Task 3 (复杂但必需)
3. **低优先**: Task 4 (辅助性)

### 复用代码

- 表格组件可复用 `Leaderboard.tsx` 中的逻辑
- 弹窗可复用 `RiskConfirmationModal` 中的结构
- API调用集中在 `lib/` 文件中

### 测试策略

1. 本地开发测试 (mock数据)
2. CNC Mainnet测试网验证
3. 手机端适配测试

---

**状态**: 📋 准备就绪  
**估计工作量**: 7.5 小时  
**优先级**: P0 (阻塞测试网上线)
