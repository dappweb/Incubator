# ============================================================
# Incubator — Environment Variable Template
# Copy to .env and fill in all required values before running.
# Never commit .env to version control.
# ============================================================

# ─── Deployer / Hardhat ─────────────────────────────────────
# Private key of the deployer wallet (no 0x prefix)
DEPLOYER_PRIVATE_KEY=

# CNC Mainnet RPC
CNC_MAINNET_RPC_URL=https://rpc.cncchainpro.com

# ─── Deployed Contract Addresses ────────────────────────────
# Token addresses (must use real deployed token addresses)
USDT_TOKEN_ADDRESS=
ICO_TOKEN_ADDRESS=
LIGHT_TOKEN_ADDRESS=

# UUPS proxy addresses (set after deploy, used by upgrade scripts)
INCUBATOR_CORE_PROXY=
NODE_OTC_MARKET_PROXY=
SWAP_POOL_MANAGER_PROXY=
IDENTITY_NFT_PROXY=
PRIMARY_SWAP_CONTROLLER_PROXY=

# ─── Pool Recipient Wallets ──────────────────────────────────
# Each pool recipient must be a non-zero address.
# Set to the contract address itself (INCUBATOR_CORE_PROXY) to enable
# on-chain settlement via settleLeaderboard / settleNodeRewards / settleSuperNodeRewards.
LP_POOL_ADDRESS=
REFERRAL_POOL_ADDRESS=
SUPER_NODE_POOL_ADDRESS=
NODE_POOL_ADDRESS=
PLATFORM_POOL_ADDRESS=
LEADERBOARD_POOL_ADDRESS=

# Backward-compatible aliases (optional)
POOL_LIQUIDITY_RECIPIENT=
POOL_REFERRAL_RECIPIENT=
POOL_SUPER_NODE_RECIPIENT=
POOL_NODE_RECIPIENT=
POOL_PLATFORM_RECIPIENT=
POOL_LEADERBOARD_RECIPIENT=

# OTC fee receiver wallet
OTC_FEE_RECIPIENT=

# ─── Deploy Script Flags ─────────────────────────────────────
# true = seed initial swap pool liquidity after deploy
SEED_SWAP_LIQUIDITY=false

# ─── Frontend (Vite) — must be prefixed with VITE_ ───────────
VITE_USDT_CONTRACT_ADDRESS=
VITE_USDT_DECIMALS=18
VITE_ICO_TOKEN_ADDRESS=
VITE_LIGHT_TOKEN_ADDRESS=
VITE_CORE_CONTRACT_ADDRESS=
VITE_OTC_CONTRACT_ADDRESS=
VITE_SWAP_POOL_ADDRESS=
VITE_PRIMARY_SWAP_CONTROLLER_ADDRESS=

# DEX addresses on CNC Mainnet (required only if primary USDT/ICO swap should use an external router)
VITE_PANCAKE_V2_ROUTER_ADDRESS=
VITE_PANCAKE_V2_FACTORY_ADDRESS=
# Fee in PPM (parts-per-million); PancakeSwap V2 default = 2500 (0.25%)
VITE_PANCAKE_V2_PRIMARY_FEE_PPM=2500

# Operational helper for primary swap sell-gate scripts
ICO_HOLDER_COUNT=

# Optional CNC explorer metadata override
VITE_CNC_MAINNET_BLOCK_EXPLORER_URL=https://cncchainpro.com

# ─── Scheduled Settlement (Optional) ─────────────────────────
# LIGHT fee settlement
SWAP_LIGHT_PAIR_ID=1
LIGHT_SETTLE_MIN_AMOUNT=0

# Core pool settlement switches
ENABLE_NODE_SETTLEMENT=true
ENABLE_SUPER_NODE_SETTLEMENT=true
ENABLE_LEADERBOARD_SETTLEMENT=true

# Comma-separated recipients and matching BPS shares (sum must be 10000)
NODE_REWARD_RECIPIENTS=
NODE_REWARD_SHARES=
SUPER_NODE_REWARD_RECIPIENTS=
SUPER_NODE_REWARD_SHARES=

# Leave empty to auto-settle yesterday's dayId
LEADERBOARD_DAY_ID=

# JSONBin announcement source
# Frontend read path (safe/public):
VITE_JSONBIN_ANNOUNCEMENTS_BIN_ID=
VITE_JSONBIN_ANNOUNCEMENTS_ACCESS_KEY=
# Optional override, default is https://api.jsonbin.io/v3
VITE_JSONBIN_API_BASE_URL=https://api.jsonbin.io/v3

# Server-side write key (never use VITE_ prefix, never expose to browser)
JSONBIN_API_KEY=
JSONBIN_ANNOUNCEMENTS_BIN_ID=
